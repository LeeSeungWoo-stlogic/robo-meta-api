# --- web ---
fastapi>=0.115,<1.0
uvicorn[standard]>=0.30,<1.0
pydantic>=2.0,<3.0
requests>=2.0,<3.0
httpx>=0.27,<1.0
sqlglot>=27.0,<31.0

# --- graph / db ---
neo4j>=5.25,<6.0
psycopg[binary,pool]>=3.2,<4.0
asyncpg>=0.30,<1.0

# --- llm / embedding ---
openai>=2.0,<3.0

# --- env ---
python-dotenv>=1.0,<2.0

# --- semantic view v2 (JWT 인증·consumer contract 시험) ---
PyJWT[crypto]>=2.9,<3.0
jsonschema>=4.23,<5.0
pytest>=8.0,<10.0

# --- subject_area 분류 규칙 (K-AIR 차용) ---
PyYAML>=6.0,<7.0
