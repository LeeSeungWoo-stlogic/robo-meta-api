# --- web ---
fastapi>=0.115,<1.0
uvicorn[standard]>=0.30,<1.0
pydantic>=2.0,<3.0
requests>=2.0,<3.0

# --- graph / db ---
neo4j>=5.25,<6.0
psycopg[binary,pool]>=3.2,<4.0
asyncpg>=0.30,<1.0

# --- llm / embedding ---
openai>=2.0,<3.0

# --- env ---
python-dotenv>=1.0,<2.0

# --- subject_area 분류 규칙 (K-AIR 차용) ---
PyYAML>=6.0,<7.0
